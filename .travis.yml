language: objective-c

cache: bundler

rvm:
  - 2.0.0
  - 2.1.5
  - 2.2.2

script:
    - bundle exec rake build
    - sudo gem install ./pkg/kitchenplan*
    - kitchenplan setup --gitrepo https://github.com/kitchenplan/kitchenplan-config-test.git
    - kitchenplan provision --debug
#    - sudo rake test
